pre-commit:
  parallel: true
  exclude:
    - "pnpm-lock.yaml"
  commands:
    eslint:
      glob: "src/**/*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: |
        bun lint:fix {staged_files}
      stage_fixed: true
